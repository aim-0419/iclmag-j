# 의존성
node_modules/
.pnp
.pnp.js

# 빌드 결과물
.next/
out/
build/
dist/
*.tsbuildinfo

# Claude Code 로컬 설정
# 이 폴더의 설정 파일에는 "허용한 명령어"가 그대로 기록됩니다.
# 그 명령어에 DB 비밀번호 같은 값이 포함될 수 있어(실제로 과거에 그런 일이 있었습니다)
# 아예 git 에 올리지 않도록 제외합니다.
.claude/

# 환경변수 (비밀번호 포함 - 절대 git 에 올리지 않음)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 업로드된 이미지 (서버에 직접 저장되므로 git 에는 포함하지 않음)
public/uploads/

# 캐시
.eslintcache
.swc/

# 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS 파일
.DS_Store
Thumbs.db

# Prisma
prisma/migrations/
